const PlayingMode = (function(){
    const playingMode = document.querySelector('.playing-mode');
    const humanMode = document.querySelector('.human-mode');
    const botMode = document.querySelector('.bot-mode');

    function humanModeChoosing(){
        
    }
})();